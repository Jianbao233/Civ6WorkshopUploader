# Civ6 Workshop Uploader — Template

Placeholder file to keep the `content/` directory non-empty in a fresh workspace.

Replace this whole directory with your Civ6 mod content before uploading:
`<ModName>.modinfo`, `Binaries/`, `Data/`, `UI/`, `Script/`, `Text/`, ...